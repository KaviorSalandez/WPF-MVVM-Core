<UserControl x:Class="WPFCore.App.Modules.Menus.Views.MenuEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WPFCore.App.Modules.Menus.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MenuEditViewModel}"
             d:DesignWidth="500" d:DesignHeight="450">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" />

        <Grid Grid.Row="1" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Tên menu -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Tên menu:*" VerticalAlignment="Center" Margin="0,0,0,12" />
            <syncfusion:SfTextBoxExt Grid.Row="0" Grid.Column="1" Text="{Binding MenuTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <!-- ActionKey -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="ActionKey:" VerticalAlignment="Center" Margin="0,0,0,12" />
            <syncfusion:SfTextBoxExt Grid.Row="1" Grid.Column="1" Text="{Binding ActionKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" MinHeight="36" VerticalContentAlignment="Center"/>

            <!-- Parent -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Menu cha:" VerticalAlignment="Center" Margin="0,0,0,12" />
            <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,12"
                      ItemsSource="{Binding AvailableParents}"
                      DisplayMemberPath="Title"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding ParentId, Mode=TwoWay}" MinHeight="36" VerticalContentAlignment="Center"/>

            <!-- SortOrder -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Sắp xếp:" VerticalAlignment="Center" Margin="0,0,0,12" />
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SortOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" MinHeight="36" VerticalContentAlignment="Center"/>

            <!-- Glyph -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Icon (Glyph):" VerticalAlignment="Center" Margin="0,0,0,12" />
            <syncfusion:SfTextBoxExt Grid.Row="4" Grid.Column="1" Text="{Binding Glyph, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" MinHeight="36" VerticalContentAlignment="Center"/>

            <!-- IsEnabled -->
            <TextBlock Grid.Row="5" Grid.Column="0" Text="Trạng thái:" VerticalAlignment="Center" Margin="0,0,0,12" />
            <CheckBox Grid.Row="5" Grid.Column="1" Content="Cho phép hiển thị" IsChecked="{Binding IsEnabled, Mode=TwoWay}" Margin="0,0,0,12" />
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Hủy" Command="{Binding CancelCommand}" Width="80" Margin="0,0,8,0" Padding="8,4" />
            <Button Content="Lưu" Command="{Binding SaveCommand}" Width="80" Padding="8,4" />
        </StackPanel>

        <syncfusion:SfBusyIndicator Grid.RowSpan="3" IsBusy="{Binding IsBusy}" IsHitTestVisible="{Binding IsBusy}" />
    </Grid>
</UserControl>
