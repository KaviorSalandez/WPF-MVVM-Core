<UserControl x:Class="WPFCore.App.Modules.Maps.Views.ViewMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
             xmlns:vm="clr-namespace:WPFCore.App.Modules.Maps.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:ViewMapViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Thanh công cụ (Toolbar) -->
        <ToolBar Grid.Row="0" Padding="4">
            <Button Command="{Binding LoadFileCommand}" Content="Mở file bản đồ..." Margin="2"/>
            <Separator />
            <Button Command="{Binding ZoomInCommand}" Content="Zoom In" Margin="2"/>
            <Button Command="{Binding ZoomOutCommand}" Content="Zoom Out" Margin="2"/>
            <Button Command="{Binding ResetViewCommand}" Content="Reset View" Margin="2"/>
        </ToolBar>

        <!-- MapView control -->
        <esri:MapView x:Name="MainMapView" Grid.Row="1" Map="{Binding Map}" />
    </Grid>
</UserControl>
