<UserControl x:Class="WPFCore.App.Modules.Dashboard.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WPFCore.App.Modules.Dashboard.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding Title}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,16" Foreground="#FF333333"/>
            <TextBlock Text="Chức năng hiển thị biểu đồ và số liệu báo cáo sẽ được cập nhật tại đây." FontSize="16" HorizontalAlignment="Center" Foreground="#FF666666"/>
        </StackPanel>
    </Grid>
</UserControl>
